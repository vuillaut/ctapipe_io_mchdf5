channels:
  - default
  - cta-observatory
  - conda-forge
dependencies:
  - python=3.7 # nail the python version, so conda does not try upgrading / dowgrading
  - numpy>=1.15.4
  - numpydoc
  - pytest
  - pytables
  - ctapipe=0.7
  - pip
  - pip:
      - pytest_runner
